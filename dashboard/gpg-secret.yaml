apiVersion: 'kubernetes-client.io/v1'
kind: ExternalSecret
metadata:
  name: sfa-dash-gpg-secret
spec:
  backendType: systemManager
  data:
    - key: dash-gpg-key-id
      name: gpg-key-id
    - key: dash-gpg-password
      name: passphrase.txt
    - key: dash-gpg-secret-key
      name: key.gpg
